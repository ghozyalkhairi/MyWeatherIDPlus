import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  teksKota: {
    fontSize: 16,
    marginLeft: 10,
  },
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#1C1D4B',
    paddingVertical: 12,
    paddingHorizontal: 6,
    marginVertical: 8,
  },
});
